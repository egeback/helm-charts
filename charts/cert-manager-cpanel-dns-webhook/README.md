# cert-manager-cpanel-dns-webhook

![Version: 1.0.1](https://img.shields.io/badge/Version-1.0.1-informational?style=flat-square) ![AppVersion: v0.3.0](https://img.shields.io/badge/AppVersion-v0.3.0-informational?style=flat-square)

Cert-manager webhook plugin to support ACME DNS challenges for CPanel DNS zones

**This chart is not maintained by the upstream project and any issues with the chart should be raised [here](https://github.com/egeback/helm-charts/issues/new/choose)**

## Source Code

* <https://github.com/jamesorlakin/cert-manager-cpanel-dns-webhook>

## Requirements

Kubernetes: `>=1.16.0-0`

## Dependencies

| Repository | Name | Version |
|------------|------|---------|
| https://charts.jetstack.io | cert-manager | v1.19.3 |

## TL;DR

```console
helm repo add egeback https://github.com/egeback/helm-charts
helm repo update
helm install cert-manager-cpanel-dns-webhook egeback/cert-manager-cpanel-dns-webhook
```

## Installing the Chart

To install the chart with the release name `cert-manager-cpanel-dns-webhook`

```console
helm install cert-manager-cpanel-dns-webhook egeback/cert-manager-cpanel-dns-webhook
```

## Uninstalling the Chart

To uninstall the `cert-manager-cpanel-dns-webhook` deployment

```console
helm uninstall cert-manager-cpanel-dns-webhook
```

The command removes all the Kubernetes components associated with the chart **including persistent volumes** and deletes the release.

## Configuration

Read through the [values.yaml](./values.yaml) file. It has several commented out suggested values.
Other values may be used from the [values.yaml](https://github.com/bjw-s/helm-charts/blob/main/charts/library/common/values.yaml).

Specify each parameter using the `--set key=value[,key=value]` argument to `helm install`.

```console
helm install cert-manager-cpanel-dns-webhook \
  --set env.TZ="America/New York" \
    egeback/cert-manager-cpanel-dns-webhook
```

Alternatively, a YAML file that specifies the values for the above parameters can be provided while installing the chart.

```console
helm install cert-manager-cpanel-dns-webhook egeback/cert-manager-cpanel-dns-webhook -f values.yaml
```

## Custom configuration

N/A

## Values

**Important**: When deploying an application Helm chart you can add more values from our common library chart [here](https://github.com/bjw-s/helm-charts/tree/main/charts/library/common)

| Key | Type | Default | Description |
|-----|------|---------|-------------|
| affinity | object | `{}` |  |
| cert-manager | object | `{"enabled":false,"installCRDs":true}` | cert-manager dependency configuration |
| cert-manager.enabled | bool | `false` | install cert-manager as a subchart |
| cert-manager.installCRDs | bool | `true` | install cert-manager CRDs |
| certManager.namespace | string | `"cert-manager"` | namespace where cert-manager is installed |
| certManager.renderTemplates | bool | `true` | whether to render the internal PKI resources (Issuer/Certificate) this is required for the webhook to work but might need to be disabled if you manage these resources externally or during initial install if CRDs are not present. |
| certManager.serviceAccountName | string | `"cert-manager"` | service account used by cert-manager |
| fullnameOverride | string | `""` |  |
| groupName | string | `"jameslakin.co.uk"` |  |
| image.pullPolicy | string | `"IfNotPresent"` | image pull policy |
| image.repository | string | `"jamesorlakin/cert-manager-cpanel-dns-webhook"` | image repository |
| image.tag | string | Chart.appVersion | image tag |
| livenessProbe.enabled | bool | `true` |  |
| nameOverride | string | `""` |  |
| nodeSelector | object | `{}` |  |
| readinessProbe.enabled | bool | `true` |  |
| replicaCount | int | `1` |  |
| resources | object | `{}` |  |
| service.port | int | `443` |  |
| service.type | string | `"ClusterIP"` |  |
| tolerations | list | `[]` |  |

## Changelog

### Version 1.0.1

### Older versions

A historical overview of changes can be found on [ArtifactHUB](https://artifacthub.io/packages/helm/egeback/cert-manager-cpanel-dns-webhook?modal=changelog)

## Support
- Open an [issue](https://github.com/egeback/helm-charts/issues/new/choose)

----------------------------------------------
Autogenerated from chart metadata using [helm-docs v1.14.2](https://github.com/norwoodj/helm-docs/releases/v1.14.2)
